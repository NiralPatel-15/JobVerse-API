const fs = require("fs");

const extractTextFromResume = async (filePath) => {
  return `
    React Node.js MongoDB Express
    Software Engineer
  `;
};

module.exports = {
  extractTextFromResume,
};
